import { ReserveBitsPipe } from './reserve-bits.pipe';

describe('ReserveBitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReserveBitsPipe();
    expect(pipe).toBeTruthy();
  });
});
