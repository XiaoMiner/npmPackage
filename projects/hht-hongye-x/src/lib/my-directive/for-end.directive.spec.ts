import { ForEndDirective } from './for-end.directive';

describe('ForEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ForEndDirective();
    expect(directive).toBeTruthy();
  });
});
